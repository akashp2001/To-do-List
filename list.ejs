<!-- header and footer repeated over web pages can be included in this way using EJS layouts -->

<%- include("header"); -%>

<!-- include this file in the views folder and name this file in the same way as it had been rendered in the app.js file
    include the variable name inside the ejs templates-><%= %> -->
<!-- eg->   <h1><%= title%></h1> the tags -->

<!--  the below tags enclosing the js code helps in including js control flow inside the html file %> -->

<!-- <%  if(title==="sunday" || title==="saturday"){%>
      <h1 style="color:purple;">Its a weekend....We can have FUNZZZ!!!</h1>
  <%  }else{%>
      <h1 style="color:red;">Its a weekday....blehhh!!!...I gotta work....</h1>
  <%  } %> -->
<div id="heading">
  <h1><%= title %></h1>
</div>

<div class="box">
  <!-- loop through the array passed through the EJS template and make more list subdivisions as the user enters more values -->
  <%    for(var i=0;i<newListItem.length;i++){            %>
  <form action="/delete" method="post">
    <div class="item">
      <input name="deletePurpose" class="aka" type="checkbox" onchange="this.form.submit()" value="<%= newListItem[i]._id %>">
      <p> <%= newListItem[i].name %> </p>
    </div>
    <input type="hidden" name="backendPurpose" value="<%= title %>">
  </form>
  <%    }  %>

  <form class="item" action="/" method="post">
    <!-- to set autocomplete to off -->
    <input type="text" name="text1" placeholder="ADD" autocomplete="off">
    <!-- the value of the button is set to the title of the web page using EJS marquee-->
    <!-- this value method was used before adding database -->
    <button type="submit" name="submit123" value="<%= title %>">+</button>
  </form>

</div>

<%- include("footer"); -%>
<!-- header and footer repeated over web pages can be included in this way using EJS layouts -->
